<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <label>First Name</label><%= f.text_field :firstName %><br>
  <label>Last Name</label><%= f.text_field :lastName %><br>
  <label>Email</label><%= f.text_field :email %><br>
  <label>Phone</label><%= f.text_field :phone %><br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
