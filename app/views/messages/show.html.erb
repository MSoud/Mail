<p id="notice"><%= notice %></p>

<table>
  <thead>
    <tr>
      <th>Sender</th>
      <th>Reciever</th>
      <th>Content</th>
    </tr>
  </thead>

  <tbody>
      <tr>
        <td><%= @message.sender %></td>
        <td><%= @message.reciever %></td>
        <td><%= @message.content %></td>
      
      </tr>
  </tbody>
</table>


<%= link_to 'Edit', edit_message_path(@message) %> | 
<%= link_to 'Back', user_messages_path(User.get_email_by_id @message.reciever) %>
